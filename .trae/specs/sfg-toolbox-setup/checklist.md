# Checklist

- [x] 后端项目 `backend/main.py` 可正常启动，`/api/health` 返回 `{"status": "ok"}`
- [x] 前端项目 `frontend/` 由 Vite + React + TypeScript 构建，`npm run dev` 可正常启动
- [x] Vite 开发代理配置正确，前端 `/api/*` 请求可转发到后端 `http://localhost:8000`
- [x] 前端路由配置正确：`/`、`/data-processing`、`/calculator`、`/database` 四个路由均可用
- [x] 顶部导航栏包含 "SFG Tool Box" 品牌名（点击跳回主页）和三个功能链接
- [x] 主页展示三个功能入口卡片/按钮，点击可跳转到对应页面
- [x] SFG数据处理页面（`/data-processing`）显示页面标题和占位内容
- [x] SFG计算器页面（`/calculator`）显示页面标题和占位内容
- [x] SFG数据库页面（`/database`）显示页面标题和占位内容
- [x] 导航栏有基本样式（深色背景、白色文字、hover 效果）
- [x] 页面整体布局美观，有基本的响应式适配
